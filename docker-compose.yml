version: '3.8'

services:
  web:
    build: .
    command: /start.sh
    volumes:
      - .:/app  # 挂载整个项目目录，实现代码热重载
      - sqlite_data:/app/db_data  # SQLite数据库持久化存储
    ports:
      - "8000:8000"
    env_file:
      - .env.docker
    restart: unless-stopped

volumes:
  sqlite_data: