version: '3.8'

services:
  web:
    build: .
    command: /start.sh
    volumes:
      - .:/app  # 挂载整个项目目录，实现代码热重载
    ports:
      - "8001:8000"
    env_file:
      - .env.docker
    restart: unless-stopped

volumes:
  sqlite_data: